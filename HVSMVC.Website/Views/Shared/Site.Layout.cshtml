<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    @Styles.Render("~/css/combined")
    @RenderSection("HeaderContent", false)
   

    <meta name="robots" content="index, follow" />
    <meta name="description" content="" />
    <meta name="keywords" content="" />
    <meta name="author" content="" />

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <base href="@Request.Url.Scheme://@Request.Url.Authority@Url.Content("~/")" />
    <link rel="SHORTCUT ICON" href="@Url.Content("~/favicon.ico")" type="image/x-icon" />
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top hidden-print" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="@Url.RouteUrl(Routes.DEFAULT, new { action = MVC.Blog.ActionNames.Index, controller = MVC.Blog.Name })">HVS </a>
            </div>
            <div class="collapse navbar-collapse navbar-ex1-collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.RouteLink(@Common.Blog, Routes.DEFAULT, new { action = MVC.Blog.ActionNames.Index, controller = MVC.Blog.Name })</li>
                    <li>@Html.ActionLink(@Common.About, MVC.About.Index())</li>
                </ul>

                <form method="get" class="navbar-form navbar-right" action="@Url.RouteUrl(Routes.DEFAULT, new { action = MVC.Blog.ActionNames.Index, controller = MVC.Blog.Name })">
                    <div class="form-group">
                        @Html.TextBox("search", null, new { placeholder = Common.Search, @class = "form-control" })
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="container">
        @RenderSection("HeroUnit", false)

        <ul class="breadcrumb hidden-print">@Html.MvcSiteMap().SiteMapPath()</ul>
        <h1>@Html.MvcSiteMap().SiteMapTitle()</h1>
        @RenderBody()

        <hr />
        <footer class="text-center">
            @{Html.RenderPartial(MVC.Shared.Views._LogonControl);}
        </footer>
    </div>
    @Scripts.Render("~/bundles/scripts")
    @RenderSection("ScriptContent", false)
</body>
</html>
